import 'package:flutter/material.dart';

///
/// Update the colors below according to your use case.
///

const Color scaffoldBackgroundColor = Color(0xFF000000);
const Color otherColor = Color(0xFF000000);

//dashboard color

const primaryColor = Color(0xFF2697FF);
const secondaryColor = Color(0xFF2A2D3E);
const bgColor = Color(0xFF212332);

const creamColor = Color(0xFFFFFFFF);
const snowColor = Color(0xFFFFFAFA);
